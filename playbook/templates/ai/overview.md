# Product Overview

A local macOS app acting as an LLM mediator and local API gateway.

## Core goals
- expose local API compatible with selected LLM calls
- route requests between providers
- track usage and limits
- present status in menu bar UI

## Non-goals
- cloud sync in v1
- web UI
- cross-platform support in v1
